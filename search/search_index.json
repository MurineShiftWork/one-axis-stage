{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"one-axis-stage","text":"<p>Hardware design and software for modular, low-cost one-axis stages.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install one-axis-stage\n</code></pre>"},{"location":"#usage","title":"Usage","text":"<p>See <code>tests/</code> for usage examples.</p>"},{"location":"api/","title":"API Reference","text":""},{"location":"api/#one_axis_stage.api.StageAPI","title":"<code>one_axis_stage.api.StageAPI</code>","text":"<p>               Bases: <code>StageSerialConnection</code></p> <p>Direct command interface to the stage controller firmware.</p> <p>Wraps StageSerialConnection with typed commands for device discovery, position control, velocity, operating mode, and LED feedback. Each method corresponds to one firmware command.</p>"},{"location":"api/#one_axis_stage.api.StageAPI.flash","title":"<code>flash(device_id, duration_ms, repeats)</code>","text":"<p>Trigger the LED on a device for visual identification.</p> <p>Parameters:</p> Name Type Description Default <code>device_id</code> <code>int</code> <p>Target device ID.</p> required <code>duration_ms</code> <code>int</code> <p>On-time of each flash in milliseconds.</p> required <code>repeats</code> <code>int</code> <p>Number of times to flash.</p> required"},{"location":"api/#one_axis_stage.api.StageAPI.get_info","title":"<code>get_info(device_id)</code>","text":"<p>Return a status dict for a single device.</p> <p>Baud rate and operating mode integer codes are resolved to human-readable strings and added as <code>baud_rate</code> and <code>operating_mode</code> keys alongside the raw integer originals.  A <code>connected</code> boolean is set to <code>False</code> when the bus returns the 0xFFFF no-device sentinel for <code>model_number</code>.</p> <p>Parameters:</p> Name Type Description Default <code>device_id</code> <code>int</code> <p>Dynamixel device ID on the serial bus.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>Dict with at minimum the keys <code>connected</code> (bool), <code>model_number</code>,</p> <code>dict</code> <p><code>id</code>, <code>baud_rate</code> (str), <code>baud_rate_int</code>, <code>operating_mode</code></p> <code>dict</code> <p>(str), <code>operating_mode_int</code>, <code>position_raw</code>, and <code>velocity_max</code>.</p> <code>dict</code> <p>All values reflect firmware state at call time.</p>"},{"location":"api/#one_axis_stage.api.StageAPI.get_info_all","title":"<code>get_info_all(device_ids)</code>","text":"<p>Return a list of status dicts, one per device ID.</p>"},{"location":"api/#one_axis_stage.api.StageAPI.get_position","title":"<code>get_position(device_id)</code>","text":"<p>Return the current raw position of a device.</p>"},{"location":"api/#one_axis_stage.api.StageAPI.get_stage_id","title":"<code>get_stage_id()</code>","text":"<p>Query and cache the controller's stage ID.</p>"},{"location":"api/#one_axis_stage.api.StageAPI.scan_for_devices","title":"<code>scan_for_devices(timeout=8.0, idle_timeout=3.0)</code>","text":"<p>Broadcast a scan and return a newline-separated list of discovered device IDs.</p> <p>Reads until there has been <code>idle_timeout</code> seconds of silence on the bus or <code>timeout</code> seconds total have elapsed. The firmware scans multiple baud rates in sequence; idle_timeout must be long enough to span the gap between consecutive baud rate scans (default 3 s).</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>Maximum total wait time in seconds.</p> <code>8.0</code> <code>idle_timeout</code> <code>float</code> <p>Stop reading after this many seconds with no new data.</p> <code>3.0</code>"},{"location":"api/#one_axis_stage.api.StageAPI.set_baudrate","title":"<code>set_baudrate(device_id, current_baudrate, new_baudrate)</code>","text":"<p>Change the baud rate stored on a device.</p> <p>Parameters:</p> Name Type Description Default <code>device_id</code> <code>int</code> <p>Target device ID.</p> required <code>current_baudrate</code> <code>int</code> <p>Active baud rate used for this transaction.</p> required <code>new_baudrate</code> <code>int</code> <p>Baud rate to persist on the device.</p> required"},{"location":"api/#one_axis_stage.api.StageAPI.set_device_id","title":"<code>set_device_id(current_device_id, new_device_id)</code>","text":"<p>Reassign the Dynamixel ID stored on a device.</p> <p>Parameters:</p> Name Type Description Default <code>current_device_id</code> <code>int</code> <p>Existing device ID used to address the device.</p> required <code>new_device_id</code> <code>int</code> <p>Replacement ID to persist on the device.</p> required"},{"location":"api/#one_axis_stage.api.StageAPI.set_operating_mode","title":"<code>set_operating_mode(device_id, op_mode)</code>","text":"<p>Set the operating mode of a device.</p> <p>Parameters:</p> Name Type Description Default <code>device_id</code> <code>int</code> <p>Target device ID.</p> required <code>op_mode</code> <code>str | int</code> <p>Mode name (e.g. <code>\"OP_POSITION\"</code>) or integer code.</p> required"},{"location":"api/#one_axis_stage.api.StageAPI.set_position","title":"<code>set_position(device_id, position)</code>","text":"<p>Command a device to move to an absolute raw position.</p>"},{"location":"api/#one_axis_stage.api.StageAPI.set_position_multiple","title":"<code>set_position_multiple(position_tuples)</code>","text":"<p>Command multiple devices to move simultaneously.</p> <p>Parameters:</p> Name Type Description Default <code>position_tuples</code> <code>list[tuple[int, int]]</code> <p>List of (device_id, position) pairs sent in a single packet.</p> required"},{"location":"api/#one_axis_stage.api.StageAPI.set_velocity","title":"<code>set_velocity(device_id, velocity)</code>","text":"<p>Set the velocity limit for a device (0-254).</p>"},{"location":"api/#one_axis_stage.controller.StageController","title":"<code>one_axis_stage.controller.StageController</code>","text":"<p>Coordinates one or more StageAxis instances over a shared serial connection.</p> <p>Provides named-position storage, multi-axis simultaneous moves, and YAML-based configuration save/load.  The recommended entry point is <code>from_config</code> rather than direct construction.</p>"},{"location":"api/#one_axis_stage.controller.StageController.config","title":"<code>config</code>  <code>property</code> <code>writable</code>","text":"<p>Current controller state as a serialisable dictionary.</p>"},{"location":"api/#one_axis_stage.controller.StageController.add_axis","title":"<code>add_axis(axis_name, **axis_config)</code>","text":"<p>Register a new axis on the controller and query its initial state.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Logical name used to address the axis (e.g. <code>\"x\"</code>).</p> required <code>**axis_config</code> <p>Keyword arguments forwarded to StageAxis (id, position_min, position_max, velocity_max, operating_mode).</p> <code>{}</code> <p>Returns:</p> Type Description <code>StageAxis</code> <p>The newly created StageAxis instance.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an axis with the same name already exists.</p>"},{"location":"api/#one_axis_stage.controller.StageController.connect","title":"<code>connect()</code>","text":"<p>Open the serial connection and query the stage ID.</p>"},{"location":"api/#one_axis_stage.controller.StageController.from_config","title":"<code>from_config(config_file)</code>  <code>staticmethod</code>","text":"<p>Construct a StageController from a YAML config file or a pre-loaded dict.</p> <p>Parameters:</p> Name Type Description Default <code>config_file</code> <code>str | Path | dict</code> <p>Path to a YAML configuration file, or a dict with the same structure.</p> required <p>Returns:</p> Type Description <code>StageController</code> <p>A connected StageController with axes initialised.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If a path is given but the file does not exist.</p> <code>TypeError</code> <p>If config_file is not a str, Path, or dict.</p>"},{"location":"api/#one_axis_stage.controller.StageController.move_to_known_position","title":"<code>move_to_known_position(position_name)</code>","text":"<p>Move to a previously saved named position.</p> <p>Parameters:</p> Name Type Description Default <code>position_name</code> <code>str</code> <p>Key used when the position was saved with <code>save_as_known_position</code>.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the position name is not found in <code>known_positions</code>.</p>"},{"location":"api/#one_axis_stage.controller.StageController.move_to_position","title":"<code>move_to_position(position)</code>","text":"<p>Move one or more axes to target raw positions in a single command.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>dict</code> <p>Mapping of axis name to target position. Value may be an integer raw position or a dict with a <code>\"position_raw\"</code> key.</p> required Example <p><code>ctrl.move_to_position({\"x\": 300, \"y\": 500})</code></p>"},{"location":"api/#one_axis_stage.controller.StageController.ping_axes","title":"<code>ping_axes()</code>","text":"<p>Refresh device info for all registered axes.</p>"},{"location":"api/#one_axis_stage.controller.StageController.remove_known_position","title":"<code>remove_known_position(position_name)</code>","text":"<p>Delete a named position from the known positions registry.</p>"},{"location":"api/#one_axis_stage.controller.StageController.save_as_known_position","title":"<code>save_as_known_position(position_name)</code>","text":"<p>Query all axes and store their current positions under a named key.</p> <p>Parameters:</p> Name Type Description Default <code>position_name</code> <code>str</code> <p>Label for the saved position.</p> required"},{"location":"api/#one_axis_stage.controller.StageController.save_config","title":"<code>save_config(config_file, overwrite=True)</code>","text":"<p>Write the current controller configuration to a YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>config_file</code> <code>str | Path</code> <p>Destination path.</p> required <code>overwrite</code> <code>bool</code> <p>If False, raise FileExistsError when the file already exists.</p> <code>True</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the file was written successfully.</p>"},{"location":"api/#one_axis_stage.axis.StageAxis","title":"<code>one_axis_stage.axis.StageAxis</code>","text":"<p>Represents one motor axis with configurable position limits and velocity cap.</p> <p>Wraps StageAPI calls for a specific device ID and enforces min/max position bounds on every move command before sending it to the controller.</p>"},{"location":"api/#one_axis_stage.axis.StageAxis.get_info","title":"<code>get_info()</code>","text":"<p>Query the device and update local state (position, velocity, operating mode).</p> <p>Returns:</p> Type Description <code>dict</code> <p>Status dict from the firmware for this device.</p>"},{"location":"api/#one_axis_stage.axis.StageAxis.get_position","title":"<code>get_position()</code>","text":"<p>Query and return the current raw position, updating the local cache.</p>"},{"location":"api/#one_axis_stage.axis.StageAxis.set_operating_mode","title":"<code>set_operating_mode(op_mode)</code>","text":"<p>Set the operating mode for this axis.</p> <p>Parameters:</p> Name Type Description Default <code>op_mode</code> <code>str | int</code> <p>Mode name (e.g. <code>\"OP_POSITION\"</code>) or integer code.</p> required"},{"location":"api/#one_axis_stage.axis.StageAxis.set_position","title":"<code>set_position(position)</code>","text":"<p>Move to an absolute raw position, clamped to [position_min, position_max].</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <code>int</code> <p>Target raw position in device units.</p> required <p>Raises:</p> Type Description <code>AssertionError</code> <p>If position is outside the configured bounds.</p>"},{"location":"api/#one_axis_stage.axis.StageAxis.set_velocity","title":"<code>set_velocity(velocity)</code>","text":"<p>Set the velocity limit, clamped to [0, velocity_max].</p> <p>Parameters:</p> Name Type Description Default <code>velocity</code> <code>int</code> <p>Target velocity in device units.</p> required <p>Raises:</p> Type Description <code>AssertionError</code> <p>If velocity exceeds the axis velocity_max.</p>"},{"location":"api/#one_axis_stage.axis.StageAxis.to_dict","title":"<code>to_dict()</code>","text":"<p>Return axis configuration and current state as a plain dictionary.</p>"},{"location":"api/#one_axis_stage.interface.MoveInterface","title":"<code>one_axis_stage.interface.MoveInterface</code>","text":"<p>Attaches per-axis increment helpers to a StageController.</p> <p>For each axis registered on the controller, four bound methods are created dynamically at construction time:</p> <ul> <li><code>&lt;axis&gt;p</code>  - move forward by small_increment</li> <li><code>&lt;axis&gt;m</code>  - move backward by small_increment</li> <li><code>&lt;axis&gt;pp</code> - move forward by large_increment</li> <li><code>&lt;axis&gt;mm</code> - move backward by large_increment</li> </ul> Example <p>With axes <code>x</code> and <code>y</code> registered, the following are available::</p> <pre><code>move.xp()   # small step forward on x\nmove.ymm()  # large step backward on y\n</code></pre>"},{"location":"api/#one_axis_stage.interface.MoveInterface.move_axis_by_increment","title":"<code>move_axis_by_increment(axis_name, direction_forward=True, fast_mode=False)</code>","text":"<p>Move a named axis by one increment step.</p> <p>Uses small_increment when fast_mode is False, large_increment otherwise. Out-of-bounds moves are silently swallowed and logged as errors.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the axis to move.</p> required <code>direction_forward</code> <code>bool</code> <p>True to increase position, False to decrease.</p> <code>True</code> <code>fast_mode</code> <code>bool</code> <p>If True, use large_increment instead of small_increment.</p> <code>False</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If axis_name is not registered on the controller.</p>"},{"location":"cli/","title":"CLI reference","text":"<p>Installing <code>one-axis-stage</code> registers the <code>stage</code> command.</p> <p>For interactive jog mode also install the <code>cli</code> extra:</p> <pre><code>pip install \"one-axis-stage[cli]\"\n</code></pre>"},{"location":"cli/#stage-scan","title":"stage scan","text":"<p>Broadcast a scan and print discovered device IDs.</p> <pre><code>stage scan --port PORT [--baudrate BAUD]\n</code></pre> Flag Default Description <code>--port</code> required Serial port (e.g. <code>/dev/ttyUSB0</code>, <code>COM3</code>). <code>--baudrate</code> <code>115200</code> Connection baud rate. <p>Example:</p> <pre><code>stage scan --port /dev/ttyUSB0\n</code></pre>"},{"location":"cli/#stage-info","title":"stage info","text":"<p>Print status (baud rate, operating mode, position) for a single device.</p> <pre><code>stage info --port PORT --id ID [--baudrate BAUD] [--json]\n</code></pre> Flag Default Description <code>--port</code> required Serial port. <code>--id</code> required Device ID (integer). <code>--baudrate</code> <code>115200</code> Connection baud rate. <code>--json</code> off Emit raw JSON instead of formatted table. <p>Example:</p> <pre><code>stage info --port /dev/ttyUSB0 --id 21\nstage info --port /dev/ttyUSB0 --id 21 --json\n</code></pre> <p>!!! note     If no device responds at the given ID, the command prints an error to stderr     and exits with code 1.  Use <code>stage scan</code> first to confirm which IDs are     present on the bus.</p>"},{"location":"cli/#stage-move","title":"stage move","text":"<p>Move a device to an absolute raw position, then print the confirmed position.</p> <pre><code>stage move --port PORT --id ID --position POS [--baudrate BAUD]\n</code></pre> Flag Default Description <code>--port</code> required Serial port. <code>--id</code> required Device ID. <code>--position</code> required Target position (raw units, integer). <code>--baudrate</code> <code>115200</code> Connection baud rate. <p>!!! note     No soft limits are enforced here. Confirm the valid range with     <code>stage info</code> before moving to an untested position.</p> <p>Example:</p> <pre><code>stage move --port /dev/ttyUSB0 --id 21 --position 400\n</code></pre>"},{"location":"cli/#stage-set-id","title":"stage set-id","text":"<p>Reassign the device ID stored on a device.</p> <pre><code>stage set-id --port PORT --id CURRENT_ID --new-id NEW_ID [--baudrate BAUD]\n</code></pre> <p>!!! warning     After this command the device responds only on the new ID.     Reconnect with <code>--id NEW_ID</code>.</p>"},{"location":"cli/#stage-set-baudrate","title":"stage set-baudrate","text":"<p>Change the baud rate persisted on a device.</p> <pre><code>stage set-baudrate --port PORT --id ID --current-baud CUR --new-baud NEW\n</code></pre> <p>Valid baud rates: <code>9600</code>, <code>57600</code>, <code>115200</code>, <code>1000000</code>.</p> <p>!!! warning     After this command the device responds only on the new baud rate.     Reconnect with <code>--baudrate NEW</code>.</p>"},{"location":"cli/#stage-jog","title":"stage jog","text":"<p>Interactive keyboard jog mode. Requires <code>pip install \"one-axis-stage[cli]\"</code>.</p> <pre><code>stage jog --port PORT --id ID [--baudrate BAUD]\n          [--small STEP] [--large STEP] [--min MIN] [--max MAX]\n\nstage jog --config CONFIG_YAML [--small STEP] [--large STEP]\n</code></pre> <p>Two launch modes:</p> <p>Bare mode (<code>--port</code> + <code>--id</code>): quick single-device jog with optional soft limits.</p> <p>Config mode (<code>--config</code>): load a <code>StageController</code> YAML for named multi-axis jog.</p>"},{"location":"cli/#keyboard-map","title":"Keyboard map","text":"<p>The same key scheme applies in both bare mode and config mode. The first three axes are mapped to key groups in order:</p> Key Axis Action <code>w</code> / <code>s</code> ids[0] / first axis (y) Step +/- <code>a</code> / <code>d</code> ids[1] / second axis (x) Step +/- Up / Down arrow ids[2] / third axis (z) Step +/- <code>-</code> / <code>_</code> - Halve step size <code>+</code> / <code>=</code> - Double step size <code>p</code> - Refresh position from hardware <code>q</code> / Ctrl-C - Quit <p>The status line updates after every move (one entry per axis):</p> <pre><code>y(id=21)=  599  0 [-----|---------] 1023   |   x(id=22)=  300  0 [---|------] 1023   step=20\n</code></pre> <p>In config mode the axis names from the YAML file are shown instead of <code>y</code>/<code>x</code>/<code>z</code>.</p>"},{"location":"cli/#flags","title":"Flags","text":"Flag Default Description <code>--port</code> required (bare) Serial port. <code>--id</code> required (bare) One or more device IDs. All move together. <code>--baudrate</code> <code>115200</code> Connection baud rate. <code>--config</code> \u2014 Path to StageController YAML (multi-axis mode). <code>--small</code> <code>20</code> Small step size (raw units). <code>--large</code> <code>40</code> Large step size (raw units, 2x small). <code>--min</code> <code>0</code> Soft lower position limit (bare mode). <code>--max</code> <code>1023</code> Soft upper position limit (bare mode)."},{"location":"getting_started/","title":"Getting Started","text":""},{"location":"getting_started/#installation","title":"Installation","text":"<pre><code>pip install one-axis-stage\n</code></pre> <p>The package requires Python 3.10 or later and depends on pyserial.</p>"},{"location":"getting_started/#hardware-connection","title":"Hardware connection","text":"<p>Connect the stage controller to a USB or RS-232 serial port on your computer. On Linux the port is typically <code>/dev/ttyUSB0</code>; on macOS it appears as <code>/dev/tty.usbserial-*</code>; on Windows it is a <code>COM</code> port such as <code>COM3</code>.</p> <p>Set the baud rate to match the firmware setting (default: <code>115200</code>).</p>"},{"location":"getting_started/#first-connection","title":"First connection","text":"<p>The recommended entry point is <code>StageController</code>. You can connect directly by passing a serial port and then adding axes manually:</p> <pre><code>from one_axis_stage.controller import StageController\n\nctrl = StageController(serial_port=\"/dev/ttyUSB0\", baudrate=115200, timeout=1)\n\n# Register an axis: give it a logical name and the Dynamixel device ID\nctrl.add_axis(\"x\", id=21, position_min=200, position_max=800, velocity_max=0, operating_mode=\"OP_POSITION\")\n</code></pre>"},{"location":"getting_started/#minimal-working-example","title":"Minimal working example","text":"<pre><code>from one_axis_stage.controller import StageController\n\nctrl = StageController(serial_port=\"/dev/ttyUSB0\", baudrate=115200, timeout=1)\nctrl.add_axis(\"x\", id=21, position_min=200, position_max=800, velocity_max=0, operating_mode=\"OP_POSITION\")\n\n# Query the current position\npos = ctrl.axes[\"x\"].get_position()\nprint(f\"Current position: {pos}\")\n\n# Move to an absolute raw position\nctrl.move_to_position({\"x\": 500})\n\n# Save the current location and return to it later\nctrl.save_as_known_position(\"home\")\nctrl.move_to_position({\"x\": 700})\nctrl.move_to_known_position(\"home\")\n</code></pre>"},{"location":"getting_started/#loading-a-configuration-file","title":"Loading a configuration file","text":"<p>For multi-axis rigs it is more practical to define axes in a YAML file and load with <code>from_config</code>:</p> <pre><code># stage_config.yaml\nconnection:\n  serial_port: /dev/ttyUSB0\n  baudrate: 115200\n  timeout: 1\n\naxes:\n  x:\n    id: 21\n    position_min: 200\n    position_max: 800\n    velocity_max: 0\n    operating_mode: OP_POSITION\n  y:\n    id: 55\n    position_min: 200\n    position_max: 800\n    velocity_max: 0\n    operating_mode: OP_POSITION\n\nknown_positions:\n  home:\n    x:\n      position_raw: 400\n    y:\n      position_raw: 400\n</code></pre> <pre><code>from one_axis_stage.controller import StageController\n\nctrl = StageController.from_config(\"stage_config.yaml\")\nctrl.move_to_known_position(\"home\")\n</code></pre>"},{"location":"usage/","title":"Usage","text":""},{"location":"usage/#moving-axes","title":"Moving axes","text":""},{"location":"usage/#single-axis-absolute-move","title":"Single-axis absolute move","text":"<pre><code>ctrl.move_to_position({\"x\": 500})\n</code></pre>"},{"location":"usage/#multi-axis-simultaneous-move","title":"Multi-axis simultaneous move","text":"<p>All target positions are sent in a single serial packet so the axes start moving at the same time:</p> <pre><code>ctrl.move_to_position({\"x\": 300, \"y\": 600, \"z\": 250})\n</code></pre>"},{"location":"usage/#direct-axis-access","title":"Direct axis access","text":"<p>Individual axes expose <code>set_position</code> and <code>get_position</code> directly:</p> <pre><code>axis = ctrl.axes[\"y\"]\nprint(axis.get_position())   # raw position int\naxis.set_position(450)\n</code></pre>"},{"location":"usage/#named-positions","title":"Named positions","text":"<p>Save and recall named positions to repeat common locations without hardcoding coordinates:</p> <pre><code># Save wherever the stage is right now\nctrl.save_as_known_position(\"loading\")\n\n# Move elsewhere, then return\nctrl.move_to_position({\"x\": 700, \"y\": 700})\nctrl.move_to_known_position(\"loading\")\n\n# Remove a saved position\nctrl.remove_known_position(\"loading\")\n</code></pre> <p>Named positions survive a <code>save_config</code> / <code>from_config</code> round-trip.</p>"},{"location":"usage/#saving-and-reloading-configuration","title":"Saving and reloading configuration","text":"<pre><code># Write current state (axes, known positions, connection params) to YAML\nctrl.save_config(\"my_stage.yaml\")\n\n# Reload in a later session\nctrl = StageController.from_config(\"my_stage.yaml\")\n</code></pre>"},{"location":"usage/#incremental-moves-with-moveinterface","title":"Incremental moves with MoveInterface","text":"<p><code>MoveInterface</code> is useful for interactive fine-tuning. It wraps a controller and exposes per-axis step methods:</p> <pre><code>from one_axis_stage.interface import MoveInterface\n\nmove = MoveInterface(ctrl, small_increment=20, large_increment=40)\n\nmove.xp()    # x +20\nmove.xm()    # x -20\nmove.xpp()   # x +40\nmove.xmm()   # x -40\n\n# Same pattern for every registered axis\nmove.yp()\nmove.ymm()\n</code></pre> <p>Out-of-bounds moves are caught and logged rather than raising an exception, so these helpers are safe to call in loops or GUI callbacks.</p>"},{"location":"usage/#device-identification-and-scanning","title":"Device identification and scanning","text":"<p>Flash the LED on a device to identify it physically:</p> <pre><code>ctrl.api.flash(device_id=21, duration_ms=500, repeats=3)\n</code></pre> <p>Scan the bus to discover which device IDs are online:</p> <pre><code>results = ctrl.api.scan_for_devices()\nprint(results)\n</code></pre>"},{"location":"usage/#changing-operating-mode","title":"Changing operating mode","text":"<pre><code># By name\nctrl.api.set_operating_mode(device_id=21, op_mode=\"OP_VELOCITY\")\n\n# By integer code\nctrl.api.set_operating_mode(device_id=21, op_mode=3)\n</code></pre> <p>Available modes: <code>OP_POSITION</code>, <code>OP_EXTENDED_POSITION</code>, <code>OP_CURRENT_BASED_POSITION</code>, <code>OP_VELOCITY</code>, <code>OP_PWM</code>, <code>OP_CURRENT</code>.</p>"},{"location":"usage/#logging","title":"Logging","text":"<p>The package uses the standard <code>logging</code> module. Enable debug output to see every command sent over the serial link:</p> <pre><code>import logging\nlogging.basicConfig(level=logging.DEBUG)\n</code></pre>"}]}